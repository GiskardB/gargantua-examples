# ═══════════════════════════════════════════════════════════════════
# LLM Routing Rules Example — Environment (fully-local Ollama)
#
# `mvn test` does NOT need this file — it boots in the embedded profile
# and pins the routing logic via direct LlmRouter calls.
# ═══════════════════════════════════════════════════════════════════

MONGODB_URI=mongodb://localhost:27017/agent-example-llm-routing
REDIS_URL=redis://localhost:6379

LLM_PRIMARY_PROVIDER=ollama
LLM_PRIMARY_MODEL=llama3.2:3b
LLM_PRIMARY_API_KEY=
LLM_PRIMARY_ENDPOINT=http://localhost:11434

LLM_FALLBACK_PROVIDER=ollama
LLM_FALLBACK_MODEL=qwen2.5:3b
LLM_FALLBACK_API_KEY=
LLM_FALLBACK_ENDPOINT=http://localhost:11434

LLM_ROUTING_PROVIDER=ollama
LLM_ROUTING_MODEL=phi4-mini
LLM_ROUTING_API_KEY=
LLM_ROUTING_ENDPOINT=http://localhost:11434
