# mvn test does NOT need this file — the example wires mocked ChatModel
# beans for the `primary` and `precise` aliases so no real LLM call is made.
MONGODB_URI=mongodb://localhost:27017/agent-example-skill-llm-overrides
REDIS_URL=redis://localhost:6379

LLM_PRIMARY_PROVIDER=openai
LLM_PRIMARY_MODEL=gpt-4o
LLM_PRIMARY_API_KEY=
