package ai.gargantua.example.skillllmoverrides;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Main entry point for the skill-level LLM overrides example. */
@SpringBootApplication
public class SkillLlmOverridesApplication {

    public static void main(String[] args) {
        SpringApplication.run(SkillLlmOverridesApplication.class, args);
    }
}
